import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'u.media',
  appName: 'u-media',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
